---
interface Props {
  date: Date
}

const { date } = Astro.props
const asDate = new Date(date)
---

<time datetime={asDate.toISOString()}>
  {
    asDate.toLocaleDateString('pt-br', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
